syntax = "proto3";
package api;
option go_package = "pb;api";

import "localization.proto";

// GRPC server working only with read operations from external clients
// They can not update, delete or insert data
service FreonService {
  rpc GetLocalizations (GetLocalizationsReq) returns (GetLocalizationsRes);
}

// get localizations - full list localizations code
// get translations by localization id
// get translation by id/localizartion